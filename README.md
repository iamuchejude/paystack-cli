### PAYSTACK CLI
> Simple CLI payment app

### Tehnologies
Node, inquirer

### How to test locally?
* Clone the repository
* `cd` to the repo folder
* Run `npm install`
* Setup `.env` using `.env.sample`
* Run `npm start`

### Author
Uche Jude
